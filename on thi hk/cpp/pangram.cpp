#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int MOD = (int)1e9 + 7;

int main(){
    ios_base::sync_with_stdio(0); 
    cin.tie(NULL);cout.tie(NULL);
    int t; cin >> t;
    while(t--){
        int a[200] = {0};
        string a; cin >> a;
        int x; cin >> x;
        for (int i = 0; i < a.length(); i++){
            x[a[i]]++;
        }
    }
}